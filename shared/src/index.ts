/**
 * Oko Shared Protocol
 * 
 * Single source of truth for all message types between backend and extension.
 * Both sides import from this package to ensure type safety.
 */

export * from './messages'
export * from './validation'
